Changes applied on 2025-12-18:

- Added Google Fonts (Poppins + Inter) to pages for improved typography.
- Updated `css/styles.css`:
  - Switched accent color to pink.
  - Enabled font smoothing.
  - Set headings to use Poppins.
  - Changed `.consultation` background to match page gradient (pink) and enlarged Calendly widget.
  - Centered contact section and constrained form width.
  - Adjusted responsive breakpoints so the 3rd card stays under left column until very narrow screens.
- Updated `portfolio/index.html`:
  - Removed inline Calendly sizing (widget now sized via CSS).
  - Added Calendly placeholder URL (replace with your event URL).
  - Added contact form and services blocks.
